# InfractiVision — Dependencias (variante CPU)
# Para Windows/Linux/macOS SIN GPU NVIDIA, o para CI.
# Igual que requirements.txt pero con torch/torchvision CPU (PyPI), sin
# el índice extra de CUDA. La app funcionará con CPU (más lenta) y la
# barra de estado mostrará "CPU" en vez de "GPU".
#
# ⚠️  Mismo pin crítico que requirements.txt: numpy 1.26.4 + opencv 4.9.0.80
#   (torch 1.13 exige numpy<2; opencv 5.x exige numpy>=2).

# === NÚCLEO (pins críticos de compatibilidad) ===
numpy==1.26.4
scipy==1.11.4
torch==1.13.1
torchvision==0.14.1
scikit-learn==1.4.2
scikit-image==0.23.2

# === VISIÓN POR COMPUTADOR ===
opencv-python==4.9.0.80
pillow==10.4.0
ultralytics==8.4.120
matplotlib==3.9.4

# === TRACKING (infrastructure) ===
deep-sort-realtime==1.3.2

# === UI COMPONENTS ===
tkcalendar==1.6.1

# === DATA PROCESSING ===
pandas==2.1.4
openpyxl==3.1.5

# === CLOUD & NETWORK ===
requests==2.32.3
python-dotenv==1.0.1
firebase-admin==7.5.0
google-cloud-storage==3.13.1
google-cloud-firestore==2.28.1
flask==3.1.3

# === UTILIDADES ===
psutil==5.9.8

# === BUILD TOOLS ===
pyinstaller==6.11.1